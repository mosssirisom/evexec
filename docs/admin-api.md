# Admin API

The admin area needs a protected endpoint to read quote and contact leads.

Authentication uses an admin password stored in the ADMIN_PASSWORD environment variable.

The admin dashboard is located at /admin/leads.
